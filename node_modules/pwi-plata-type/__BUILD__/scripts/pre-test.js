"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("../libs/fs");
const node_path_1 = __importDefault(require("node:path"));
const main = async () => {
    const plataDir = node_path_1.default.resolve('.');
    {
        const srcAssetsFolder = node_path_1.default.join(plataDir, 'src', 'tests', 'assets');
        const buildAssetsFolder = node_path_1.default.join(plataDir, '__BUILD__', 'tests', 'assets');
        if (await fs_1.PlataFs.access(buildAssetsFolder)) {
            await fs_1.PlataFs.fs.rm(buildAssetsFolder, { force: true, recursive: true, maxRetries: 1, retryDelay: 1 });
        }
        await fs_1.PlataFs.fs.mkdir(buildAssetsFolder, { recursive: true });
        const err = await fs_1.PlataFs.cp(srcAssetsFolder, buildAssetsFolder, { force: true, recursive: true });
        if (err !== null) {
            console.log(err);
            process.exit(1);
        }
    }
};
main();
//# sourceMappingURL=pre-test.js.map